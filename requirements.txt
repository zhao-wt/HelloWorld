streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
altair>=5.0.0
requests>=2.28.0
xlrd>=2.0.1
openpyxl>=3.1.0
yfinance>=0.2.0

# Offline model-fitting pipeline only (phases 1-5 + bear.inference fit step).
# The dashboard runtime does NOT need these -- it reads precomputed
# bear/<kind>_model_params.json with numpy/pandas only.
scikit-learn>=1.3.0
scipy>=1.10.0
statsmodels>=0.14.0
